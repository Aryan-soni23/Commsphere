// Top-level build file where you can add configuration options common to all sub-projects/modules.
// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '8.2.0' apply false // Check your actual version
    id 'com.android.library' version '8.2.0' apply false // Check your actual version
    id 'org.jetbrains.kotlin.android' version '1.8.10' apply false // Update this line
}


task clean(type: Delete) {
    delete rootProject.buildDir
}